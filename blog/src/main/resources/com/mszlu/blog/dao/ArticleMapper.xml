<?xml version="1.0" encoding="UTF-8" ?>
<!--MyBatis配置文件-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mszlu.blog.dao.ArticleMapper">
    <select id="getArticleByViewCounts" resultType="com.mszlu.blog.dao.pojo.Article">
        select id,title from `ms_article` order by view_counts desc limit #{limit};
    </select>

    <select id="getArticleByCreateDate" resultType="com.mszlu.blog.dao.pojo.Article">
        select id,title from `ms_article` order by create_date desc limit #{limit};
    </select>

    <select id="listArticles" resultType="com.mszlu.blog.dao.dos.Archives">
        select
            year(from_unixtime(create_date/1000)) as year,
            month(from_unixtime(create_date/1000)) as month,
            count(*) as count
        from `ms_article`
        group by year,month
        order by year desc,month desc;
    </select>
</mapper>