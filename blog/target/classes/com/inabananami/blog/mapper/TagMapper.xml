<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.inabananami.blog.mapper.TagMapper">

    <select id="findTagsByArticleId" parameterType="long" resultType="com.inabananami.blog.pojo.Tag">
        select id, avatar, tag_name as tagName from tag
        where id in (select tag_id from article_tag where article_id=#{articleId})
    </select>
</mapper>